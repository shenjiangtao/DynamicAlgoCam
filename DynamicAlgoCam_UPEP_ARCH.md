# 跨芯片平台 双目+激光雷达感知动态算法加载平台架构设计

## 一、总体定位

**DynamicAlgoCam 统一感知执行平台（Unified Perception & Execution Platform, UPEP）**
目标：屏蔽X86/ARM/MIPS/RISC-V等CPU架构差异，兼容GPU/NPU异构加速；统一接入双目相机、激光雷达（LiDAR）、激光雷达相机（LiDAR-Camera一体化传感器）；支持**动态模块化算法加载、运行时任务热切换、多模态融合（视觉+点云）**；支持USB/GSML/PCIe/以太网等传感器总线；内置视频编码用于数据采集录存。

> 
> 核心关键词：跨架构抽象层、异构计算调度、动态插件化算法、多模态同步、统一IO总线适配、数据录制编码子系统。

### 设计原则

1. **CPU架构无关**：业务算法与底层指令集解耦，依赖抽象层，不直接写X86/ARM/RISC-V汇编或平台专属系统调用。
2. **异构硬件透明**：GPU/NPU作为可插拔计算后端，算法只描述算子需求，由框架自动调度。
3. **动态加载**：算法模块可在运行时加载/卸载/切换，无需重启进程，支持任务流热切换（例如从障碍物检测切换到SLAM）。
4. **时间严格同步**：双目图像、LiDAR点云、IMU硬件时间戳对齐，为融合提供基础。
5. **多总线统一抽象**：USB/GSML/PCIe/Eth封装成统一传感器设备接口，上层感知算法无感。
6. **可录制编码**：原始图像/点云支持实时H.264/H.265编码存储，用于数据集采集、回放仿真。

---

# 二、分层架构（自底向上）

```
┌─────────────────────────────────────────────────────────┐
│ 应用层：感知任务编排、业务逻辑、结果输出（ROS/原生API）     │
├─────────────────────────────────────────────────────────┤
│ 算法动态加载&任务调度层（核心：插件管理器、运行时任务引擎） │
├─────────────────────────────────────────────────────────┤
│ 多模态融合引擎（视觉-LiDAR融合，标定管理，时空同步）      │
├─────────────────────────────────────────────────────────┤
│ 异构计算抽象层（GPU/NPU/CPU算子调度，跨硬件后端）         │
├─────────────────────────────────────────────────────────┤
│ 传感器抽象层（统一设备接口：双目、LiDAR、LiDAR相机）      │
├─────────────────────────────────────────────────────────┤
│ 总线适配层：USB / GSML / PCIe / Ethernet                 │
├─────────────────────────────────────────────────────────┤
│ 操作系统适配层（跨OS：Linux，RTOS；跨CPU架构兼容）        │
└─────────────────────────────────────────────────────────┘
```

## 2.1 OS & 架构适配层（最底层，屏蔽X86/ARM/MIPS/RISC-V）

**目标：消除不同CPU架构、操作系统的系统调用差异**

1. 基础封装：内存管理、线程/协程、信号量、文件IO、时钟、共享内存、原子操作。
2. 编译构建体系：CMake + 交叉编译工具链，一套源码，编译产出多架构插件。
   - 插件格式：使用**位置无关代码PIC**，Linux下`.so`；RTOS可使用ELF可重定位模块。
   - 禁止平台硬编码：区分**平台相关驱动**和**平台无关业务代码**。
3. 时间源统一：提供系统时钟、硬件PTP时间接口，用于传感器时间戳同步。
4. 内存抽象：统一虚拟内存、DMA内存分配接口（PCIe、USB大块数据传输需要DMA）。

> 
> 依赖：libc封装、跨平台线程库；RISC-V/MIPS注意ABI对齐，避免结构体内存对齐坑。

## 2.2 总线适配层

对物理传输链路做统一封装，向上输出统一`SensorStream`数据流接口：

| 总线类型 | 适配说明 | 适用传感器 |
| --- | --- | --- |
| USB2/USB3 | libusb抽象；支持批量/ISO传输；支持USB双目相机、小型固态激光雷达 | 双目相机、微型LiDAR |
| GSML | 自定义/厂商GSML协议封装，适配车载串行传感器链路 | 车载一体化LiDAR相机 |
| PCIe | DMA环形缓冲区，低延迟大数据流；支持PCIe相机、高速LiDAR | 高分辨率双目、机械激光雷达 |
| Ethernet | PTP时间同步，TCP/UDP，支持GigE/10GigE相机、网络LiDAR | 远程传感器、工业感知设备 |

功能：

- 总线故障检测、断线重连、带宽监控、流量控制。
- 将原始总线字节流解析为帧数据包（图像帧 / 点云包），打上原始硬件时间戳，向上传递。

## 2.3 传感器抽象层（Sensor HAL）

**统一设备模型，上层感知代码完全不关心传感器硬件型号和接入总线**
设备类型：

1. **双目相机设备**：左图、右图、内参、外参、曝光控制、帧率控制。输出：成对图像帧 + 时间戳。
2. **LiDAR设备**：点云（x,y,z,intensity,ring），点云时间戳，扫描参数。
3. **LiDAR-Camera一体化设备**：相机+雷达硬件同步，统一标定参数，图像与点云帧绑定。

核心接口（统一C接口，方便跨语言/插件调用）

```
// 伪代码统一接口
typedef struct SensorDevice SensorDevice;
int sensor_open(SensorHandle* handle, const char* uri); // uri可自动识别usb:// pcie:// eth:// gsml://
int sensor_start_stream(SensorHandle handle);
int sensor_read_frame(SensorHandle handle, FrameBuffer* frame); // FrameBuffer区分ImageFrame / PointCloudFrame
int sensor_get_calib(SensorHandle handle, CalibParam* calib);
int sensor_set_param(SensorHandle handle, const char* key, const char* val);
int sensor_close(SensorHandle handle);
```

- FrameBuffer：统一数据容器，支持图像、点云；支持引用计数，零拷贝传递到上层，避免多份内存拷贝。
- 标定管理：保存内参、外参、手眼标定、雷达相机联合标定参数，持久化存储。

## 2.4 异构计算抽象层（HAL for GPU/NPU/CPU）

**异构硬件透明化，算法插件只声明计算需求，框架自动分配硬件后端**
支持后端：

- CPU（X86/ARM/RISC-V/MIPS通用算子）
- GPU：CUDA、OpenCL、Vulkan（跨平台GPU，RISC-V也可以跑Vulkan）
- NPU：各厂商NPU适配层（如RK、地平线、寒武纪等，抽象成统一算子接口）

核心组件：

1. **算子注册表**：算子（卷积、池化、点云降采样、ICP、立体匹配）向各个硬件后端注册实现。
2. **计算图调度器**：算法输出计算图，调度器根据硬件负载、功耗、延迟约束选择后端。
3. **张量/点云容器**：统一Tensor、PointCloud数据结构，支持设备内存自动迁移（Host <-> GPU/NPU）。
4. 资源隔离：每个算法任务独立计算上下文，防止任务之间显存/NPU资源抢占。

> 
> 关键：算法插件**不直接调用CUDA或NPU SDK**，全部调用抽象算子API，实现跨芯片平台兼容。

## 2.5 多模态融合引擎（视觉 + LiDAR融合）

输入：双目图像、双目深度图、LiDAR点云、时间戳、标定参数
能力：

1. **时空同步模块**：基于PTP/硬件触发时间戳做帧对齐，插值补全时间不同步的帧；输出对齐后的图像+点云帧组。
2. 融合策略库（模块化，可动态加载融合算法）
   - 前融合：图像像素与点云投影融合（LiDAR点投影到图像平面）
   - 后融合：视觉目标检测框 + LiDAR点云目标检测结果做目标关联、卡尔曼融合
   - 深度融合：双目稠密深度 + LiDAR稀疏点云联合优化深度估计
3. 坐标转换：统一世界坐标系、相机坐标系、雷达坐标系之间转换工具。
4. 融合结果输出：融合点云、3D检测框、语义点云。

## 2.6 动态算法加载 & 任务调度层【本系统核心】

### 2.6.1 插件管理器（动态加载核心）

算法模块以**独立插件包**形式存在，编译为对应架构的`.so`（ELF PIC）。

- 插件类型：双目立体匹配、图像检测、LiDAR分割、多模态融合、跟踪、SLAM等。
- 插件元信息：名称、版本、资源需求（内存/NPU算力）、输入输出数据类型、依赖算子。
- 运行时能力：
  - load / unload：动态加载、卸载，不重启主进程
  - hot-swap：任务切换时，加载新算法，完成上下文迁移，再卸载旧算法（平滑切换，无感知断流）
  - 插件签名校验：安全校验，防止恶意算法模块加载
- 插件之间解耦：模块之间通过消息总线/共享内存传递数据，不直接函数调用。

### 2.6.2 任务引擎 & 运行时任务编排

1. **任务图DAG描述**：用户/配置文件定义感知流水线DAG，例如：
`双目采集 → 立体匹配 → LiDAR采集 → 时空同步 → 视觉雷达融合 → 3D目标检测`
DAG节点就是算法插件，可以动态增删、替换节点。
2. 任务调度器：
   - 支持多任务并发/互斥；优先级调度
   - 资源限额：限制每个任务占用CPU/GPU/NPU内存，防止OOM
   - 热切换：例如任务A（障碍物检测）运行中，切换为任务B（建图SLAM），调度器逐步切换DAG，可保留部分状态（可选）
3. 数据总线：两种模式
   - 零拷贝共享内存（高性能，实时感知主链路）
   - 消息队列（异步、非实时，用于日志、编码录制、结果上报）

## 2.7 视频编码 & 数据采集子系统

用于数据集录制，采集原始双目图像、融合结果视频、点云打包存储：

1. 编码抽象层：支持硬件编码（GPU/NPU内置硬件编码器）、软件编码（CPU）。
   - 图像编码：H.264/H.265，双目左右图可以合并编码或分开编码。
   - 点云存储：PCD/ROS bag / 自定义二进制包；可和编码视频做时间索引绑定。
2. 录制控制：启停录制、分片存储、循环录制、磁盘满保护。
3. 回放模块：录制包可以离线回放，复现传感器数据流，用于算法仿真测试。> 
> 编码后端同样走异构抽象层：优先调用硬件编码器；硬件不支持时自动降级CPU软编。

## 2.8 应用层

- 对外API：C API / C++ API，可选ROS2桥接层（适配机器人生态）
- 业务模块：结果可视化、目标上报、控制指令下发、Web配置管理、算法版本管理
- 配置：JSON/YAML配置传感器、DAG任务流、硬件后端选择、录制参数

---

# 三、关键数据流（实时链路）

1. 物理传感器（双目/LiDAR/LiDAR相机） → 总线层 → Sensor HAL，生成带时间戳FrameBuffer
2. 多模态引擎做时间对齐、坐标转换
3. DAG任务调度器调用动态加载的算法插件，通过异构层调度CPU/GPU/NPU执行计算
4. 融合模块输出融合感知结果
5. 分支：
   - 主链路：感知结果输出到应用层
   - 采集分支：图像帧送入编码子系统，视频+点云打包落盘

> 
> 全链路尽量零拷贝，FrameBuffer引用计数管理，避免内存拷贝带来的延迟。

# 四、跨芯片平台工程方案

## 4.1 构建系统

CMake + 工具链文件，为X86_64、AArch64、MIPS64、RISC-V64分别交叉编译：

- 框架核心库：跨平台，一次源码多架构编译
- 算法插件：按架构独立编译，插件管理器自动根据当前CPU架构加载对应版本插件
- 异构算子：CPU算子跨架构编译；GPU/NPU算子每个硬件后端单独实现，由抽象层封装

## 4.2 二进制分发策略

主程序 + 插件仓库目录：

```
DynamicAlgoCam_upep_main
plugins/
    x86_64/
    aarch64/
    riscv64/
    mips64/
        stereo_match.so
        lidar_fusion.so
        detection.so
```

程序启动自动探测当前CPU架构，加载对应子目录插件。

## 4.3 可移植约束（必须遵守）

1. 禁止硬编码架构相关类型（不要直接用`long`，使用固定宽度`uint64_t`）
2. 所有结构体设计使用`__attribute__((packed))`谨慎，统一内存对齐策略
3. 所有平台相关驱动代码隔离在总线/OS适配层，业务算法插件纯平台无关代码
4. 浮点数：统一使用IEEE754，注意RISC-V浮点扩展差异

# 五、非功能性设计

1. **实时性**：支持RT-Linux；任务优先级、线程亲和性绑定；低延迟路径采用共享内存。
2. **容错**：传感器掉线自动重连；算法插件崩溃隔离（沙箱/进程隔离可选），单插件崩溃不导致整个平台宕机。
3. **资源监控**：CPU、内存、GPU/NPU利用率、带宽、帧率监控；过载降帧保护。
4. **可测试**：录制包离线回放仿真，不需要真实传感器，在PC上验证算法。
5. **安全**：插件签名校验；权限控制；可选择沙箱运行动态算法。

# 六、可选扩展模块

1. 远程算法下发：通过网络动态推送新算法插件到边缘设备
2. 标定自动标定工具：双目标定、雷达相机联合标定模块
3. 性能Profiler：全链路耗时追踪，算子耗时、总线延迟、任务调度延迟统计
4. 仿真接口：接入Gazebo/Unity仿真传感器数据

# 七、潜在风险与对策

| 风险 | 对策 |
| --- | --- |
| RISC-V/MIPS NPU生态薄弱 | NPU算子可降级CPU；抽象层支持自动硬件回落策略 |
| 不同传感器时间戳不统一 | 强制硬件触发+PTP，软件插值对齐；记录时间偏差 |
| 动态插件内存泄漏 | 插件严格使用框架提供内存分配接口，卸载时框架统一回收；内存检测 |
| 多架构插件版本混乱 | 插件元信息+版本号，架构标记，加载时版本校验 |
| PCIe/USB大数据流带宽瓶颈 | DMA，环形buffer，流量控制，丢帧策略 |

---

# 八、Mermaid架构图（可直接渲染）

```
flowchart TD
    subgraph 硬件层
        S1[双目相机]
        S2[激光雷达]
        S3[LiDAR-Camera一体化相机]
        H1[CPU X86/ARM/MIPS/RISC-V]
        H2[GPU]
        H3[NPU]
    end

    subgraph 总线适配层
        B1[USB]
        B2[GSML]
        B3[PCIe]
        B4[Ethernet]
    end

    OS[OS&架构适配层<br/>跨CPU架构封装]
    SensorHAL[传感器抽象层<br/>双目/LiDAR统一设备API]
    Hetero[异构计算抽象层<br/>CPU/GPU/NPU算子调度]
    Fusion[多模态融合引擎<br/>时空同步+视觉激光融合]

    subgraph 动态算法&任务调度
        PluginMgr[插件管理器<br/>动态加载/卸载/热切换]
        TaskDAG[DAG任务调度引擎<br/>运行时流水线编排]
    end

    Encode[视频编码&数据采集子系统<br/>H.264/H.265 + 点云录制回放]
    App[应用层<br/>业务逻辑/结果输出/配置]

    %% 连线
    S1 & S2 & S3 --> B1 & B2 & B3 & B4
    B1 & B2 & B3 & B4 --> OS
    OS --> SensorHAL
    SensorHAL --> Fusion
    Fusion --> TaskDAG
    TaskDAG <--> PluginMgr
    TaskDAG <--> Hetero
    Hetero --> H1 & H2 & H3
    TaskDAG --> Encode
    TaskDAG --> App
```
